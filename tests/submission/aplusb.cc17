#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <map>
using namespace std;


int solveMeFirst(int a, int b) {
 // Hint: Type return a+b; below:
  return a + b;
}
int main() {
  map<string, int> m;
  int bar = 1;
  const auto [it, inserted] = m.insert( {"foo", bar} );
  int num1, num2;
  int sum;
  cin>>num1>>num2;
  sum = solveMeFirst(num1,num2);
  cout<<sum;
  return 0;
}
